# git-上传新项目

在git网页上新建项目并且勾上新建readme
然后在本地文件夹运行git bash here
然后输入 git init
然后远程连接git 输入 git remote add origin ‘url’
查看远程状态 git remote -v
获取上面代码 git pull origin master
把代码都添加git add .
提交git commit -m "介绍"
推送git push origin master
